# Project notes

This ZIP intentionally excludes node_modules so it stays small and portable.
Run `npm install` after opening it in VS Code.

The frontend uses local sample data in `src/main.jsx`. Replace that layer with a real game-state/store and backend when implementing the full simulation.
